let configSql = {
    
    host: "localhost",
    user: "root",
    password: "root",
    database : "dbprofesorado"
    
}

export default configSql

// ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root';
// FLUSH PRIVILEGES;